import React from 'react';

const Logo = ({ className }) => {
    return (
        <div>
            <span className={`${className} font-black`}>SCRIPTLY</span>
        </div>
    );
};

export default Logo;