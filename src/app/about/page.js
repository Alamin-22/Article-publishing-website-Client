import { Alert } from "flowbite-react";
const page = () => {
    return (
        <div>
            <h1>This is about page.</h1>
            <Alert color="info">Alert!</Alert>
        </div>
    );
};

export default page;